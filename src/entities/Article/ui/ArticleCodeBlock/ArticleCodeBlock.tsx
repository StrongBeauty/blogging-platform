import { useTranslation } from 'react-i18next';

export const ArticleCodeBlock = () => {
  const { t } = useTranslation();

  return (
  // eslint-disable-next-line i18next/no-literal-string
    <div>ArticleImageBlock</div>
  );
};
